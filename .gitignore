# Track beads-lite task database (overrides global gitignore)
# Worktrees inherit this, so orchestrator agents can see tasks
!.beads-lite/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
server/dist
dev-dist
dist-ssr
build
*.local

# Git worktrees for orchestrator agents
.worktrees/

# Local MCP config (copy .mcp.json.example to .mcp.json)
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/generated/

.agent-assets/

# Transcript path file (created by transcript watcher, agent-specific)
.hgnucomb-transcript-path
